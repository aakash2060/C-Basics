#include <iostream> // pre-processor command
using namespace std;

int main()
{
  cout << "Hello World!"; // print output in console
  return 0;
}

// In terminal to compile our file g++ .\Hello.cpp
// In terminal to run our file >> .\Hello.exe
// We installed code runner extension so that we don't have to do compile and run every time we want our program to run
// Code Runner exctension does that for us with a simple click
// C, C++ intellisense extension gives recommedation for our code
